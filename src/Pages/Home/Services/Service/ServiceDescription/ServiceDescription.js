import React from 'react';

const ServiceDescription = ({item}) => {
    return (
        <p>
            {
                item
            }
            
        </p>
    );
};

export default ServiceDescription;